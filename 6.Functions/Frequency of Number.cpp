// Write a function that counts how many times each digit appears in a number.
#include<iostream>
using namespace std;

int Digitappear(int arr[], int size) {

    int count=0;
    for (int i=0; i<size; i++) {
        if (arr[i] )
    }
}